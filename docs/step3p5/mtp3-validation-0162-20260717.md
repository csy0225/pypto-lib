# Step3p5 MTP3 Integration Validation — 0162 — 2026-07-17

## 1. Test object

```text
machine       gpu-a910x-0162
devices       8,9,10,11,12,13,14,15
branch        feat/whole-net-mtp3-integration
base          0e7a0fddc90c4f2348f1d59e015fb817a0877a02
checkpoint    /data/chensiyu/step3p5_flash_release_hf_mtp3_w8a8_0328-copy-mtp
main program  whole_decode_faithful_real
MTP program   whole_mtp3
main token    6127
sampler token 303
```

Cards 0–7 remained owned by the pre-existing root vLLM process throughout
the work. No process on cards 0–7 was stopped or modified.

## 2. IPC/dtype/address contract

Fresh pool:

```text
/tmp/n1_weight_ipc_mtp3_20260717
```

The eight exported maps passed:

```text
all entry offsets       512-byte aligned
main routed weights     INT8
main routed scales      FP32
MTP projection weights  BF16
MTP norm ABI            FP32
main KV                  distinct IPC entries
MTP layer 45/46/47 KV    distinct IPC slices
```

This is the native-W8A8 main path; no routed-MoE BF16 dequant fallback was
used.

## 3. Canonical main-network regression

Log:

```text
/data/chensiyu/hw_project/pypto/workspace/logs_n1/mtp3_20260717/
canonical_p42.log
```

Result:

```text
P_FAITHFUL_MOE_LAYERS=42
token=6127
native W8A8 IPC
KV IPC
rc=0
RUN done 2.61s
argmax=303
TOP5=[303,9592,768,1043,410]
```

The emitted main hidden used by MTP:

```text
/tmp/n1_mtp3_chain_20260717/P42_nh_row0.pt
```

## 4. Final MTP3 device validation

Final compile:

```text
/data/chensiyu/hw_project/pypto/workspace/build_output/
WholeMtp3_20260717_135934
```

Single-active-row log:

```text
/data/chensiyu/hw_project/pypto/workspace/logs_n1/mtp3_20260717/
mtp3_observable_single.log
```

Result:

```text
rc=0
runtime=1.94s
MTP45 token=6178
MTP46 token=410
MTP47 token=303
each token identical on all 8 TP ranks
each program token == concatenated full-logits argmax
padding hidden max=0
padding logits max=0
RESULT=MTP3_IPC_RUN_CLEAN
```

Final `active_batch=16` log:

```text
/data/chensiyu/hw_project/pypto/workspace/logs_n1/mtp3_20260717/
mtp3_observable_batch16.log
```

Result:

```text
rc=0
runtime=1.96s
all 16 rows pass TP-rank token consistency
all 16 rows pass program-token == full-logits-argmax
tokens_row0=[6178,410,303]
RESULT=MTP3_IPC_RUN_CLEAN
```

## 5. Precision

Canonical ctx=1 CPU reference:

```text
/data/chensiyu/hw_project/pypto/workspace/logs_n1/mtp3_20260717/
mtp3_observable_reference.json
```

Result:

```text
reference tokens = [6178,410,303]
device tokens    = [6178,410,303]

MTP45 logits pass rate = 1.000000000
MTP45 hidden pass rate = 1.000000000
MTP46 logits pass rate = 0.984964609
MTP46 hidden pass rate = 1.000000000
MTP47 logits pass rate = 0.976934910
MTP47 hidden pass rate = 0.998779297
worst numeric pass rate = 0.976934910
```

The focused reference mirrors PyPTO BF16/FP32 TP cast boundaries. Exact token
parity is required; the 0.97 elementwise numeric gate accounts for CPU versus
Ascend Cube accumulation differences over three chained layers.

Existing detailed vLLM MTP oracle:

```text
tests/step3p5/test_step3p5_mtp3_detail_st.py
1 passed in 311.92s
279 checks
worst pass rate >= 0.999
```

This covers the detailed 18-token vLLM dump, including Q/K/norm/projection
intermediates; it is separate from the canonical ctx=1 chain above.

## 6. Static/unit gates

```text
py_compile                 PASS
git diff --check           PASS
bash -n isolation scripts  PASS
MTP integration contracts  4 passed in 7.24s
full PyPTO + PTOAS compile PASS
```

## 7. Environment isolation and cleanup

Scripts:

```text
scripts/run_vllm_mtp3_front8.sh
scripts/run_pypto_mtp3_back8.sh
```

The front-8 script is an artifact only and was not started because cards 0–7
already hosted the root service. The back-8 script clears vLLM environment
variables and uses explicit physical devices 8–15.

After validation:

```text
fresh exporters stopped cleanly
cards 8–15 show no running process
cards 0–7 root vLLM remains alive
no relevant 507018 / running-stalled / page-fault / illegal-VA dmesg line
was observed in the final inspection
```

## 8. Unified whole-network CI runner — 2026-07-17

The new authoritative orchestration runner was executed from:

```text
tests.step3p5.run_whole_network_ci
```

Source:

```text
branch = feat/whole-net-mtp3-integration
commit = 6db80b2587af94485a6f0de09d96d75056f23675
tree   = aea559d466fc3cb6b2027253f142e65f30a9a85d
```

The runner used one fresh exporter pool and then ran all default stages:

```text
artifact_dir = /data/chensiyu/hw_project/pypto/workspace/logs_n1/mtp3_ci_20260717
pool_dir     = /tmp/n1_weight_ipc_mtp3_ci_20260717
devices      = 8,9,10,11,12,13,14,15
```

Machine result:

```text
WHOLE_NETWORK_MTP3=PASS
canonical_main_p42  rc=0  argmax=303  duration=39.567s
mtp3_single         rc=0  tokens=[6178,410,303]  duration=15.416s
mtp3_batch16        rc=0  tokens_row0=[6178,410,303]  duration=15.817s
mtp3_cpu_reference  rc=0  worst_numeric_pass_rate=0.976934910  duration=2.915s
```

The JSON report records these independent gates:

```text
main:
  hidden token 6127
  native W8A8 routed weights
  KV IPC
  P42 hidden dump
  RUN done
  argmax=303

MTP single and batch16:
  sampler handoff token 303
  exact tokens [6178,410,303]
  TP-rank/full-logits checks delegated to the device harness
  padding hidden/logits zero for the single-active-row case

reference:
  reference_tokens == device_tokens == [6178,410,303]
  ok=true
  worst_numeric_pass_rate >= 0.97
```

Cleanup gate:

```text
all exporter return codes = 0
remaining exporter PIDs = []
active exporter commands after cleanup = []
ready/STOP/key/map files removed from the pool directory
cards 8–15: no running processes
```

The runner did not send signals to or launch processes on devices 0–7. The
post-run `npu-smi` inspection still showed the pre-existing root VLLM worker
process IDs 327577, 327601, 327619, 327691, 327743, 327818, 327862, and
327907 on devices 0–7. The runner therefore records front-8 isolation as an
execution property of this run, while the continued root service ownership is
confirmed by the post-run device inspection. The shell-side `ps` check also
showed the root service parent PID 327370 and engine PID 327561 alive; the
earlier `kill -0` loop run as `infra` was not valid liveness evidence because
that user cannot signal the root-owned processes (`EPERM` also produces a
nonzero result). Only the read-only `/proc`, `ps`, and `npu-smi` checks are used
as evidence.
